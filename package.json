{
  "name": "mongoose-timestamp-events",
  "description": "Mongoose plugin that adds createdAt and updatedAt auto-assigned date properties",
  "version": "0.4.0",
  "author": "Dan Scanlon",
  "keywords": [
    "mongodb",
    "mongoose",
    "plugin",
    "timestamps",
    "createdAt",
    "updatedAt"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/danscan/mongoose-timestamp-events"
  },
  "devDependencies": {
    "mongoose": "~3.8.8",
    "should": "~3.2.0",
    "mocha": "~1.18.2"
  },
  "scripts": {
    "test": "mocha -u bdd -R spec -c ./test/*test.js"
  },
  "license": "MIT",
  "engine": {
    "node": ">= 0.6"
  }
}
